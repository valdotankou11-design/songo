[build]
builder = "nixpacks"

[deploy]
# PHP built-in server — Railway injecte $PORT automatiquement
startCommand = "php -S 0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
